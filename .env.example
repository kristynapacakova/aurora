# --- Formspree ---
NEXT_PUBLIC_FORMSPREE_ENDPOINT=

# --- Resend (alternativa k Formspree) ---
RESEND_API_KEY=
RESEND_TO_EMAIL=
RESEND_FROM_EMAIL=

# --- Supabase ---
# Najdete na https://supabase.com → váš projekt → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
# Service Role key — NIKDY nevystavujte klientovi (jen server-side API routes)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# --- Členská sekce /studium ---
# Heslo se NENASTAVUJE zde — lektorka ho mění přes /aurora-admin.
# Při prvním spuštění se řídí hodnotou vloženou do Supabase (viz README SQL).
# Tajný klíč pro podepisování session cookie klientek.
STUDIO_COOKIE_SECRET=zmente-me-na-nahodny-tajny-retezec

# --- Administrace /aurora-admin ---
# Přístupové heslo pro administraci (mění se pouze tady, ne přes UI).
ADMIN_PASSWORD=AdminAurora2026
# Tajný klíč pro podepisování admin session cookie.
ADMIN_COOKIE_SECRET=jiny-nahodny-tajny-retezec
